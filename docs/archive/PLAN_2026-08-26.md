# Plan wdrożenia na 26 sierpnia 2026

## Decyzje Marcina (26.08, rano)

| # | Temat | Decyzja |
| --- | --- | --- |
| 1 | Commity | a — 3–4 tematyczne: pamięć+dedup / threshold guard / CI / voice-eval |
| 2 | Qdrant „nie wiem” | a — `QdrantUnavailableError` |
| 3 | Przy awarii Qdranta | a — pomiń przebieg, zapisz przyczynę |
| 4 | Schema drift | a — **przeliczyć** wektory tym samym kodem co nowe |
| 5 | Duplikaty | a — najpierw snapshot + migracja schematu, **potem** dry-run |
| 6 | Gemini key w URL | b — **później** |
| 7 | Vectorscope 0.92 + G1 skrypt | a — od razu |
| 8 | Routing V2 | b — shadow + warunek z liczbami (**N / zgodność / margines — brakuje liczb**) |

## Stan kodu po decyzjach

- **A, E** — zrobione wcześniej
- **B** — **zrobione dziś**: `QdrantUnavailableError`, fail-closed w workerze, testy
- **7a** — **zrobione dziś**: Vectorscope bierze `ACTIVITY_DUPLICATE_MIN_SCORE` (0.97);
  `start-qdrant.ps1` tworzy z `--restart no`
- **G1 kontener** — nadal twoja komenda: `docker update --restart=no voiceloop-qdrant`
- **C2** — **zrobione 26.08**: 1204/1204 przeliczone; schema `memory-documents-v2` /
  `reembedded_documents_v2`; odtworzenie **100%** na próbce strażnika
- **C3/C1** — następne: próg 0.97 wygląda na `over_broad`, bo prawdziwe duplikaty
  teraz wracają na 1.000; dry-run dedup, potem zgoda na usuwanie
- **6 Gemini** — odłożone
- **8 V2** — potrzebne trzy liczby od Ciebie
- **Commity** — na twoje „commit”; podział jak w #1
- Testy: **654 passed**, lint czysty na zmienionych plikach

Zmiany nadal **nie są zacommitowane** (poza trzema lokalnymi commitami Vectorscope
przed `origin/master`).

| Blok | Stan |
| --- | --- |
| A. Strażnik progów | **zrobione** |
| E. CI | **zrobione** (niezacommitowane → CI na GH jeszcze nie działa) |
| B. Cisza jako tryb awarii | **zrobione** (2a+3a) |
| G. Restart Qdranta (skrypt) | **skrypt zrobiony**; kontener — TY |
| Vectorscope źródło prawdy 0.97 | **zrobione** (7a) |
| C. Schema + dedup | **C2 zrobione**; dalej C3 pomiar + C1 dry-run |
| D. RRF / F. handoff / Gemini | odłożone |

---

# Część 1. Twoja kolejka (zaktualizowana)

## Teraz — odblokowuje C2

### TY-S. Snapshot Qdranta (zanim cokolwiek przeliczymy)

Kontener `voiceloop-qdrant`, wolumen `voiceloop-qdrant-data`. Bez tego nie
ruszam C2. Odtworzenie z SQLite obejmuje tylko oś `semantic`.

### TY-G. Kontener już stojący

```powershell
docker update --restart=no voiceloop-qdrant
```

Skrypt dla **nowych** kontenerów jest już na `--restart no`.

### TY-V2. Trzy liczby (decyzja 8b)

Wybrałeś warunek z liczbami, ale ich nie podałeś. Potrzebuję:

- N = ile kolejnych ocenionych poleceń
- zgodność z V1 / oczekiwanym wynikiem ≥ ?
- margines ≥ ? (jest już `routing_v2_execute_min_margin=0.10` — zostawić?)

### TY-1. Commity

Gdy powiesz „commit”, dzielę tematycznie jak w decyzji #1. Nie commituję sam.

## Potem (po snapshocie)

Program: C2 przeliczenie → C3 ponowny pomiar progu → C1 dry-run dedup.
Ty: potwierdzenie dry-run przed usuwaniem.

## Odłożone świadomie

- Gemini key (6b)
- Scalanie RRF (D)
- Handoff (F1)
- Rozbicie dużych modułów

---

# Część 2. Robota programu

## Zrobione dziś (po decyzjach): blok B + 7a

### B — `QdrantUnavailableError`

- `qdrant_memory.py`: wyjątek; `has_memory` / `has_content_hash` /
  `get_memory_payload` rzucają zamiast zwracać `False`/`None`; `search` rzuca,
  gdy **wszystkie** osie padną
- `screenpipe_memory.py`: worker przy `QdrantUnavailableError` pauzuje przebieg
  (backoff), nie zapisuje kopii; `_looks_like_duplicate` i
  `_semantic_duplicate_exists` propagują wyjątek
- Testy w `test_qdrant_memory.py` i `test_screenpipe.py`

### 7a — jedno źródło prawdy + G1 skrypt

- `vectorscope/live.py`: `DEDUP_THRESHOLD = ACTIVITY_DUPLICATE_MIN_SCORE` (0.97)
- `scripts/start-qdrant.ps1`: `--restart no`

## Zrobione wcześniej: blok A — strażnik progów

Wspólny rdzeń `threshold_measure.py`, worker `threshold_guard.py`, health
`threshold_guard`. Na żywej kolekcji: min_score disabled, dedup 0.97 working,
**schema drifted** (14–21% exact reconstruction) — stąd decyzja 4a.

## Zrobione wcześniej: blok E — CI

`.github/workflows/ci.yml` + `requirements-dev.txt` + `known-first-party`.
Niezacommitowane = na GitHubie jeszcze nie działa.

## Czeka: blok C (po snapshocie)

1. **C2** — przeliczyć wektory produkcyjnym szablonem + `embed_documents` (4a)
2. **C3** — ponowny pomiar progu 0.97
3. **C1** — dry-run dedup; usuwanie tylko po Twojej zgodzie na wynik dry-run

Kolejność sztywna: snapshot → C2 → C3 → C1.

## Odłożone: D, F, Gemini

RRF nie scalać bez pomiaru; handoff po ustabilizowaniu pamięci; Gemini później.

---

# Stan weryfikacji

- [x] testy — 654
- [x] blok B fail-closed
- [x] Vectorscope = produkcyjny próg 0.97
- [x] skrypt Qdrant `--restart no`
- [x] snapshot przed C2
- [x] napływ wyłączony (`SCREENPIPE_VECTOR_MEMORY_ENABLED=false`)
- [x] C2 przeliczenie 1204 punktów — reconstruction working 100%
- [x] C1 dry-run @0.999: **84 grupy, 471 do usunięcia, zostanie 733**, max grupa 146;
  raport `data/dedupe-dry-run-c1.json`
- [x] C1 apply: **usunięto 471**; kolekcja **733**; residual @0.999 = **0 grup**
- [ ] C3: próg 0.97 nadal `over_broad` (~13.6% par ≥0.97, p99≈0.999) — to już nie
  exact-kopie, tylko bardzo podobne kubełki; ewentualna kalibracja
- [ ] przywrócić `SCREENPIPE_VECTOR_MEMORY_ENABLED=true`
- [ ] liczby V2 (N, zgodność, margines)
- [ ] commit tematyczny + push CI
